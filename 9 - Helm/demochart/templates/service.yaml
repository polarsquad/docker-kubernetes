apiVersion: v1
kind: Service
metadata:
  name: {{ include "exercise7.fullname" . }}
  labels:
    {{- include "exercise7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exercise7.selectorLabels" . | nindent 4 }}
